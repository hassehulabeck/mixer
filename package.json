{
  "name": "mixer",
  "version": "1.0.0",
  "description": "A simple pair-programming mixer, very hastily constructed.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"php -S localhost:4000 -t backend\" \"php -S localhost:8080 -t frontend\""
  },
  "author": "Hans Andersson",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
